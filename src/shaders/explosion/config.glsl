// Define should be more performant than declaring variables for readability only.
// Could probably use uniforms for these to make them tweakable from a component.
// Nice exercise for using the #define directive though.
#define ANIMATION_LENGTH 3.0
#define EXPLOSION_SCALE 12.0